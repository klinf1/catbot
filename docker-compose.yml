services:
  catbot:
    image: klinf/catbot:latest
    restart: unless-stopped
    volumes:
      - ./database:/data
      - ./logs:/logs/files
    environment:
     - PYTHONUNBUFFERED=1

volumes:
  database:
  logs:


